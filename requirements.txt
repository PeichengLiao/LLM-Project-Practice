# ============================================
# DocMind — 智能文档问答系统
# ============================================

# --- LLM & Embeddings ---
ollama>=0.4.0
openai>=1.0.0                 # 兼容 OpenAI API 格式（可选云 API）

# --- Document Processing ---
unstructured[all-docs]>=0.15.0
pymupdf>=1.24.0
python-docx>=1.1.0
markdown>=3.6
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Vector Store & Retrieval ---
chromadb>=0.5.0
rank-bm25>=0.2.2
sentence-transformers>=3.0.0

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.0.0
python-multipart>=0.0.9

# --- Async & Cache ---
redis>=5.0.0
httpx>=0.27.0

# --- Fine-tuning (for CUDA/GPU environments) ---
# Install separately on GPU machines:
# torch>=2.0.0
# transformers>=4.40.0
# peft>=0.10.0
# bitsandbytes>=0.43.0
# accelerate>=0.30.0
# datasets>=2.20.0

# --- Evaluation ---
ragas>=0.2.0

# --- Utilities ---
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
tiktoken>=0.7.0
tqdm>=4.66.0
loguru>=0.7.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
